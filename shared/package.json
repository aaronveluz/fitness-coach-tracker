{
  "name": "@boilerplate/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript types, Zod schemas, and constants used by both frontend and backend",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'No build needed for shared'",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
